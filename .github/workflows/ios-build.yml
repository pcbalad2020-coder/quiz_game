name: "Build IPA for 3uTools"

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64

      - name: Install dependencies
        run: flutter pub get

      # بناء التطبيق بصيغة نياشين iOS بدون توقيع رسمي (Unsigned)
      - name: Build Xcode Project
        run: flutter build ios --release --no-codesign

      # تجميع الملفات وتحويلها إلى صيغة .ipa صالحة للتثبيت الخارجي
      - name: Create IPA Package
        run: |
          mkdir -p Payload
          mv build/ios/iphoneos/Runner.app Payload/
          zip -r app.ipa Payload

      # رفع ملف الـ IPA النهائي إلى جيت هاب
      - name: Upload IPA to GitHub
        uses: actions/artifacts@v3
        with:
          name: my-flutter-app-ipa
          path: app.ipa